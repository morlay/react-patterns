{
  "private": true,
  "scripts": {
    "start": "gitbook serve",
    "build": "gitbook build",
    "deploy": "npm run build && gh-pages _book"
  },
  "dependencies": {
    "@morlay/gh-pages": "0.0.x",
    "gitbook-cli": "2.3.x"
  }
}